dependencies {
    api "org.jetbrains:annotations:${jetbrainsAnnotationsVersion}"
    api 'com.fasterxml.jackson.core:jackson-databind'
    api "biz.paluch.logging:logstash-gelf:${logstashGelfVersion}"

    api files('libs/openmap-5.1.15.jar')
}
